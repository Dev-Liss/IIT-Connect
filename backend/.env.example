# ====================================
# IIT CONNECT - ENVIRONMENT VARIABLES
# ====================================
# Copy this file to .env and fill in your values
# NEVER commit the actual .env file to git!

# Server port
PORT=5000

# MongoDB connection string
# Get this from MongoDB Atlas (cloud) or use local: mongodb://localhost:27017/iit-connect
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>
MONGO_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@your-cluster.mongodb.net/iit-connect?retryWrites=true&w=majority

# JWT Secret for authentication tokens (Phase 3)
# Generate a random string for production!
JWT_SECRET=your_super_secret_jwt_key_here
